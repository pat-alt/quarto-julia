---
title: |
    Quarto and `Documenter.jl`
---

## Introduction

Since `Documenter.jl` takes Markdown documents as input, you can use Quarto with `Documenter.jl` for package documentation. To make this as painless as possible, the `documenterjl-commonmark` format specifies a few relevant output option.

## Usage

**TBD**